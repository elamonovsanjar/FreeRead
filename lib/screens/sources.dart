import 'package:flutter/cupertino.dart';

class Sources extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Text('just a text, here will be many functions')
    );
  }
}
